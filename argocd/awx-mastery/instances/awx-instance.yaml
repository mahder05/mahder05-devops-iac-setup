apiVersion: awx.ansible.com/v1beta1
kind: AWX
metadata:
  name: awx-dev
spec:
  # Remove the web_service_nodeport_port line entirely
  service_type: ClusterIP
  # Keep your other specs like admin_user, postgres/redis settings, etc.
  admin_user: admin
  admin_password_secret: awx-admin-password # or however you defined it
