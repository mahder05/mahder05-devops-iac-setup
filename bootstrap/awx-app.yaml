apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: awx-stack
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
    targetRevision: HEAD
    path: infrastructure/operator  # Point this to the operator first
  destination:
    server: https://kubernetes.default.svc
    namespace: awx-mastery
  syncPolicy:
    automated:
      prune: true
      selfHeal: true